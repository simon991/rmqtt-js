{
  "name": "rmqtt-js",
  "version": "0.1.3",
  "description": "High-performance MQTT server for Node.js built with Rust and Neon",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "require": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "sideEffects": false,
  "scripts": {
    "build": "npm run clean && npm run build:native:release && npm run build:ts",
    "build:native": "cargo build --message-format=json-render-diagnostics && node scripts/copy-native.js",
    "build:native:release": "cargo build --release --message-format=json-render-diagnostics && node scripts/copy-native.js",
    "build:ts": "tsc",
    "install": "node scripts/install.js",
    "test": "npm run clean && npm run build:native && mocha --exit --require ts-node/register \"test/**/*.test.ts\"",
    "clean": "rm -rf dist && rm -rf prebuilds && cargo clean --profile release",
    "prebuild:generate": "node scripts/make-prebuild.js",
    "prepublishOnly": "npm run clean && npm run build:native:release && npm run build:ts && npm run prebuild:generate",
    "pack:check": "npm pack --dry-run",
    "example": "npm run build && node examples/simple-server.ts --experimental-strip-types"
  },
  "license": "MIT",
  "dependencies": {
    "node-gyp-build": "^4.8.4"
  },
  "devDependencies": {
    "@types/chai": "^5.2.2",
    "@types/mocha": "^10.0.10",
    "@types/mqtt": "^0.0.34",
    "@types/node": "^20.19.16",
    "chai": "^6.0.1",
    "mocha": "^10.0.0",
    "mqtt": "^5.14.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/simon991/rmqtt-js.git"
  },
  "keywords": [
    "mqtt",
    "pubsub",
    "broker",
    "server",
    "rmqtt",
    "iot",
    "websocket",
    "tls",
    "typescript"
  ],
  "author": "Simon Buchholz",
  "bugs": {
    "url": "https://github.com/simon991/rmqtt-js/issues"
  },
  "homepage": "https://github.com/simon991/rmqtt-js#readme",
  "engines": {
    "node": ">=14.0.0"
  },
  "files": [
    "dist/",
    "prebuilds/",
    "src/",
    "scripts/",
    "Cargo.toml",
    "Cargo.lock",
    "README.md",
    "LICENSE"
  ]
}
